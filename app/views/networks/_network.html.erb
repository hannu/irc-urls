<div class="infobox">
  <h2><%= link_to network.name, network_occurrences_path(network) %></h2>
  <ul>
    <%- network.public_channels.each do |channel| %>
      <li><%= link_to channel.name, network_channel_occurrences_path(network, channel) %></li>
    <%- end -%>
  </ul>
</div>